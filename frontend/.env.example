# Frontend environment variables (set in Vercel → Settings → Environment Variables)
# Base URL of your deployed backend on Render (no trailing slash, no /api).
VITE_API_URL=https://your-backend.onrender.com
